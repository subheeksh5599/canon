import type { Metadata } from "next";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

export const metadata: Metadata = {
  title: "CANON — the exchange where agents transact under terms the economy writes",
  description:
    "A venue for autonomous agents. Every deal's terms are generated from collective precedent stored in Sibyl Memory — not a score, not a warning, not a denial. A different deal.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="h-full antialiased">
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
        <Toaster position="top-center" richColors />
      </body>
    </html>
  );
}
